# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: bedard.webhooks::lang.hooks.columns.name

    executed_at:
        label: bedard.webhooks::lang.hooks.columns.executed_at
        type: timesince

    logs_count:
        label: bedard.webhooks::lang.hooks.columns.logs_count
        type: number

    url:
        cssClass: url-column
        label: bedard.webhooks::lang.hooks.columns.url
        type: partial
        path: $/bedard/webhooks/models/hook/_url.htm
        clickable: false

    is_enabled:
        label: bedard.webhooks::lang.hooks.columns.status
        type: partial
        path: $/bedard/webhooks/models/hook/_status.htm
